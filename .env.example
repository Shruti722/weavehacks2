# ==============================================================================
# REQUIRED API KEYS
# ==============================================================================

# Weights & Biases API Key (for metrics logging and artifact storage)
# Get your key from: https://wandb.ai/authorize
WANDB_API_KEY=your_wandb_api_key_here

# OpenPipe API Key (for RL training with ServerlessRL/ART)
# Get your key from: https://app.openpipe.ai/settings
OPENPIPE_API_KEY=your_openpipe_api_key_here

# ==============================================================================
# PROJECT CONFIGURATION
# ==============================================================================

# W&B Project Name (for training runs and artifacts)
WANDB_PROJECT=synergi-grid-optimization

# Weave Project Name (for LLM call tracing)
# Format: username/project-name or org/project-name
WEAVE_PROJECT=your-username/synergi-grid

# ==============================================================================
# TRAINING CONFIGURATION (for OpenPipe ServerlessRL)
# ==============================================================================

# Base model for RL fine-tuning
# Options: qwen2.5-14b, qwen2.5-7b, qwen2-72b
BASE_MODEL=qwen2.5-14b

# Episode horizon (number of timesteps per episode)
# Default: 12 steps = 1 hour at 5-minute intervals
HORIZON=12

# Dataset reference (W&B Artifact JSONL - Weave trace export)
# Seeds initial contexts from pre-collected trajectories
# Falls back to synthetic data if missing
DATASET_REF=weave-traces-ds:latest

# Artifact verification (fail early if artifact missing)
# Set to false to skip verification and use synthetic data only
VERIFY_ARTIFACT=true

# Sampling parameters (ART can override via env)
TEMPERATURE=0.3
TOP_P=0.9
